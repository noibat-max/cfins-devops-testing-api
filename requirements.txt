# qa-platform-api — runtime dependencies
# Pinned to majors that are current as of the port; lockfile/pip-tools can come later.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
boto3>=1.34,<2.0
certifi>=2024.2.2
PyJWT[crypto]>=2.8,<3.0
bcrypt>=4.1,<5.0
python-dotenv>=1.0,<2.0
